---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: data-ca
  namespace: data
spec:
  secretName: tls-data-internal-ca
  commonName: ${DOMAIN}
  isCA: true
  issuerRef:
    name: self-signed-issuer
